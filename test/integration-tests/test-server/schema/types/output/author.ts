export const Author = `type Author implements CommonEntity {
                        id: ID!
                        firstName: String,
                        lastName: String,
                        creationDate: String,
                        lastUpdateDate: String,
                        dataVersion: Int!,
                        realityId: Int!
                     }`;
