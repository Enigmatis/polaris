export const Author = `type Author implements CommonEntity {
                        id: ID!
                        firstName: String,
                        lastName: String,
                        creationDate: Date,
                        lastUpdateDate: Date,
                        dataVersion: Int!,
                        realityId: Int!
                     }`;
