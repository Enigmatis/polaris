import { CommonEntityInterface } from './entities/common-entity-interface';
import { ConnectionDirective } from './entities/connection-directive';
import { UpperCaseDirective } from './entities/upper-case-directive';

export const CommonEntities = {
    commonEntityInterface: CommonEntityInterface,
    upperCaseDirective: UpperCaseDirective,
    connectionDirective: ConnectionDirective,
};
